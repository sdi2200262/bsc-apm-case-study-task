[project]
name = "mock-cas"
version = "0.1.0"
description = "Mock CAS authentication service for the BSc APM Case Study testing environment."
requires-python = ">=3.12"
dependencies = []

[[project.authors]]
name = "CobuterMan"
email = "sdi2200262@di.uoa.gr"

[project.scripts]
mock-cas = "mock_cas.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mock_cas"]
